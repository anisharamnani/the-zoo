%ul 
	%li
		=@zookeeper.name
	%li
		=@zookeeper.quirk
	%li 
		=@zookeeper.zoo_id

= link_to("delete this guy", @zookeeper, :method => "delete", :confirm => "Are you sure")
=link_to("Back", zookeepers_path)